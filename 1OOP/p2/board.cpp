#ifndef BOARD_CPP
#define BOARD_CPP

#include "board.h"



/*void Board::setShip(PLACESHIP myShip, int thisShip) {
    myShip.shipType.length = ship[thisShip].length;
    myShip.shipType.name = ship[thisShip].name;
}*/

#endif